# Files and directories created by pub.
.dart_tool/
.packages
build/

# Output of `dart doc`.
doc/api/

# This package is a library, so its lockfile is not checked in
# (see https://dart.dev/guides/libraries/private-files#pubspeclock).
pubspec.lock
